#include"raylib.h"

#define  WIDTH 1080
#define HEIGHT 720

int main(){
  InitWindow(WIDHT,HEIGHT,"snakewindow");
  SetTargetFPS(60);

  while(!WindowShouldClose()){

    //draw
    BeginDrawing();
    ClearBackground({0.4f,0.6f,0.5f,1.0f});

    EnDrawing();
  }
}
